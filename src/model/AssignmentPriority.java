package model;

/**
 * Enum för uppdragsstatus.
 * 
 * @author kettel
 * 
 */
public enum AssignmentPriority {
	// Ett uppdrag har antingen prio hög (PRIO_HIGH), låg (PRIO_HIGH),
	// eller normal (PRIO_NORMAL) 
	PRIO_LOW, PRIO_NORMAL, PRIO_HIGH

}
